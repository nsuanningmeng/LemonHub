# LemonHub v0.4.39

修复模型广场的性能分组与当前定价分组不一致，以及已删除分组继续影响成功率的问题。

## 修复

- 性能页按模型当前启用、用户可用且已配置倍率的分组显示；没有近期请求的分组仍会列出，指标显示为无数据。
- 删除某模型的分组后，该分组从性能详情和趋势中移除；其历史记录和实时样本不再计入模型总体及近期成功率。其他模型仍可正常使用同名分组。
- 返回模型广场或重新打开详情时刷新定价与性能数据，避免缓存继续展示旧分组。

## 升级须知

- 本版本不需要数据库迁移。历史性能记录仍按原保留策略保存，展示和汇总仅采用当前可用分组。
